apiVersion: v1
kind: Secret
metadata:
  name: winpost-secrets
type: Opaque
stringData:
  # ※実際のパスワードやIPアドレスを入れてください。ローカル専用であれば平文で書ける stringData が便利です。
  # 例: postgres://winpost:winpost_dev@192.168.1.100:5432/winpost
  DATABASE_URL: "postgres://<user>:<password>@<db-ip>:5432/<dbname>"
